import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'vega-joquin-007d',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
